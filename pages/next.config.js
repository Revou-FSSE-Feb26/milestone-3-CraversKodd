/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: Change links `/about` to `/about/` for GitHub Pages compatibility
  trailingSlash: true, 
}

module.exports = nextConfig